# Deploys the cert-manager operand (controller/webhook/cainjector) after the operator CSV is ready.
apiVersion: operator.openshift.io/v1alpha1
kind: CertManager
metadata:
  name: cluster
spec:
  managementState: Managed
